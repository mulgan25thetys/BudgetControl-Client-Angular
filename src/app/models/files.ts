import { Operation } from './operation';
export class Files {
    id ? :any;
    name? :any; 
    extension? :any;	
    size ? :any;
    types ? :any;
    createdAt ? :any;
    updatedAt? :any;
    OperationId?: any;
    Operation?: Operation
}